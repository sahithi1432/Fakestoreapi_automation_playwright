{
  "name": "fakestoreapi-cart-automation",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  },
  "scripts": {
    "test": "npx playwright test",
    "test:ci": "npx playwright test",
    "report": "npx playwright show-report playwright-report"
  }
}
